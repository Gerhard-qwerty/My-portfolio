services:
  - type: web
    name: my-portfolio
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python My-portfolio.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9
